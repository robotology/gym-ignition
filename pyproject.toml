[build-system]
requires = ["setuptools>=42", "setuptools_scm[toml]>=3.4", "cmake-build-extension","wheel", "idyntree"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
local_scheme = "dirty-tag"
